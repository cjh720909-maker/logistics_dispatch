원본 MySQL
  - SELECT만 허용
  - CREATE / INSERT / UPDATE / DELETE 절대 금지

        ↓ 읽기

로컬 DB
  - CREATE 가능
  - INSERT 가능
  - UPDATE 가능
  - 우리 프로그램 전용

  
  나는 개발 초기에는 이렇게 할 것 같아.
테이블 하나만 다시 만들 때 → DROP TABLE product
DB 구조를 전부 바꿀 때 → local.db 파일 삭제